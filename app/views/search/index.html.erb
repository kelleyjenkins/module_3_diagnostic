<h1> 10 Closest Stations (within 6 miles) <h1>

  <% @results.each do |station| %>
  <%= station.name %>
  <%= station.address %>
  <%= station.fuel_type %>
  <%= station.distance %>
  <%= station.access_times %>
  <% end %>
